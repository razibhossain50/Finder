{
  "version": 2,
  "builds": [
    { "src": "frontend/package.json", "use": "@vercel/next" }
  ],
  "buildCommand": "cd frontend && npm ci && npm run build",
  "env": {
    "NODE_ENV": "production"
  }
}
