import type { Metadata } from "next";

export const metadata: Metadata = {
  title:
    "Finder - Admin Dashboard",
};

export default function Ecommerce() {
  return (
    <div className="grid grid-cols-12 gap-4 md:gap-6">
        admin dashboard
    </div>
  );
}
